*Instance и Local variables в java*

1) Instance var - переменная объявленная как переменная класса, существует до тех
пор, пока существует объект этого класса.
2) Local var - переменная объявленная внутри метода, существует, пока выполняется
этот метод.